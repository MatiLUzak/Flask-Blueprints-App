<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Lista punktów</title>
</head>
<body>
  <h1>Wszystkie rekordy w bazie</h1>
  <nav>
    <a href="{{ url_for('main.add') }}">Dodaj nowy punkt</a>
  </nav>
  <hr>

  <table border="1" cellpadding="5">
    <thead>
      <tr>
        <th>Lp.</th>
        <th>id</th>
        <th>Feature1</th>
        <th>Feature2</th>
        <th>Category</th>
        <th>Usuń</th>
      </tr>
    </thead>
    <tbody>
      {% for dp in datapoints %}
      <tr>
        <td>{{ loop.index }}</td>
        <td>{{ dp.id }}</td>
        <td>{{ dp.feature1 }}</td>
        <td>{{ dp.feature2 }}</td>
        <td>{{ dp.category }}</td>
        <td>
          <form action="{{ url_for('main.delete', record_id=dp.id) }}" method="POST">
            <button type="submit">Usuń</button>
          </form>
        </td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
</body>
</html>
